{
  "name": "ai-story-channel",
  "version": "0.1.0",
  "private": true,
  "description": "Fully automated YouTube channel: daily long-form horror/scary-facts video + teaser Short",
  "main": "scheduler.js",
  "scripts": {
    "start": "node scheduler.js",
    "run:once": "node -e \"require('./pipeline').runDailyPipeline().then(()=>process.exit(0)).catch(()=>process.exit(1))\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "groq-sdk": "^0.7.0",
    "edge-tts-universal": "^1.4.0",
    "googleapis": "^140.0.0",
    "node-cron": "^3.0.3"
  }
}
