[1mdiff --git a/pipeline.js b/pipeline.js[m
[1mindex e79a683..f3bba36 100644[m
[1m--- a/pipeline.js[m
[1m+++ b/pipeline.js[m
[36m@@ -32,7 +32,7 @@[m [masync function runPipelineForTopic(topic) {[m
 [m
   console.log('[2/6] Generating narration audio...');[m
   const audioPath = path.join(workDir, 'narration.mp3');[m
[31m-  await generateNarrationAudio(script.scenes, { outputPath: audioPath });[m
[32m+[m[32m  await generateNarrationAudio(script.scenes, { outputPath: audioPath, narrativeStyle });[m
 [m
   console.log('[3/6] Fetching/generating images...');[m
   const imagesDir = path.join(workDir, 'images');[m
[36m@@ -124,7 +124,7 @@[m [masync function runMorningPipeline(topic) {[m
   console.time('step2_audio');[m
   console.log('[2/6] Generating narration audio...');[m
   const audioPath = path.join(workDir, 'narration.mp3');[m
[31m-  await generateNarrationAudio(script.scenes, { outputPath: audioPath });[m
[32m+[m[32m  await generateNarrationAudio(script.scenes, { outputPath: audioPath, narrativeStyle });[m
 [m
   console.timeEnd('step2_audio');[m
   console.time('step3_images');[m
[1mdiff --git a/services/image.service.js b/services/image.service.js[m
[1mindex 14b6fa6..37c63c1 100644[m
[1m--- a/services/image.service.js[m
[1m+++ b/services/image.service.js[m
[36m@@ -1,18 +1,11 @@[m
 const fs = require('fs');[m
 const path = require('path');[m
[32m+[m[32mconst { spawn } = require('child_process');[m
 [m
[31m-// endpoint قديم منفصل عن gen.pollinations.ai - مجاني تماماً بدون مفتاح API،[m
[31m-// بدون حد يومي موثق، بس موديل flux بس (بدون kontext).[m
 const IMAGE_BASE = 'https://image.pollinations.ai/prompt';[m
 const IMAGE_MODEL = process.env.POLLINATIONS_IMAGE_MODEL || 'flux';[m
 const FETCH_TIMEOUT_MS = 180000;[m
 [m
[31m-const GEMINI_API_KEY = process.env.GEMINI_API_KEY;[m
[31m-const EVAL_MODEL = process.env.GEMINI_EVAL_MODEL || 'gemini-2.5-flash';[m
[31m-const GEMINI_EVAL_URL = `https://generativelanguage.googleapis.com/v1beta/models/${EVAL_MODEL}:generateContent`;[m
[31m-[m
[31m-const CANDIDATE_COUNT = Math.max(1, parseInt(process.env.IMAGE_CANDIDATE_COUNT || '2', 10));[m
[31m-[m
 const CHARACTER_REF_PATH = path.join(__dirname, '..', 'assets', 'character-reference.png');[m
 [m
 const CHARACTER_DESCRIPTION =[m
[36m@@ -36,11 +29,33 @@[m [mfunction isValidImage(filePath) {[m
   }[m
 }[m
 [m
[31m-function fileToInlinePart(filePath) {[m
[31m-  const ext = path.extname(filePath).toLowerCase();[m
[31m-  const mimeType = ext === '.jpg' || ext === '.jpeg' ? 'image/jpeg' : 'image/png';[m
[31m-  const data = fs.readFileSync(filePath).toString('base64');[m
[31m-  return { inline_data: { mime_type: mimeType, data } };[m
[32m+[m[32mfunction runFfmpeg(args) {[m
[32m+[m[32m  return new Promise((resolve, reject) => {[m
[32m+[m[32m    const proc = spawn('ffmpeg', ['-y', ...args]);[m
[32m+[m[32m    let stderr = '';[m
[32m+[m[32m    proc.stderr.on('data', (d) => (stderr += d.toString()));[m
[32m+[m[32m    proc.on('close', (code) => (code === 0 ? resolve() : reject(new Error(stderr.slice(-1500)))));[m
[32m+[m[32m  });[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m// Sharpen + slight upscale pass (no external model, pure ffmpeg unsharp filter).[m
[32m+[m[32masync function enhanceImage(filePath) {[m
[32m+[m[32m  const tmpPath = filePath.replace(/(\.\w+)$/, '.enhanced$1');[m
[32m+[m[32m  try {[m
[32m+[m[32m    await runFfmpeg([[m
[32m+[m[32m      '-i', filePath,[m
[32m+[m[32m      '-vf', "scale=iw*1.1:ih*1.1:flags=lanczos,unsharp=5:5:0.8:5:5:0.4",[m
[32m+[m[32m      tmpPath,[m
[32m+[m[32m    ]);[m
[32m+[m[32m    if (isValidImage(tmpPath)) {[m
[32m+[m[32m      fs.renameSync(tmpPath, filePath);[m
[32m+[m[32m    } else {[m
[32m+[m[32m      try { fs.unlinkSync(tmpPath); } catch {}[m
[32m+[m[32m    }[m
[32m+[m[32m  } catch (err) {[m
[32m+[m[32m    console.warn(`  -> Sharpening skipped (${err.message})`);[m
[32m+[m[32m    try { fs.unlinkSync(tmpPath); } catch {}[m
[32m+[m[32m  }[m
 }[m
 [m
 async function downloadImageResponse(res, destPath) {[m
[36m@@ -56,6 +71,7 @@[m [masync function downloadImageResponse(res, destPath) {[m
     try { fs.unlinkSync(destPath); } catch {}[m
     throw new Error('Image failed quality check');[m
   }[m
[32m+[m[32m  await enhanceImage(destPath);[m
   return destPath;[m
 }[m
 [m
[36m@@ -93,100 +109,28 @@[m [masync function generateCharacterReferenceIfMissing() {[m
   throw new Error(`Character reference generation failed after 3 attempts: ${lastError?.message}`);[m
 }[m
 [m
[31m-async function generateOneCandidate(scene, destPath) {[m
[32m+[m[32m// Best-of-N candidate picker removed: generates exactly ONE image per scene directly.[m
[32m+[m[32masync function generateSceneImageBestOf(scene, outputDir) {[m
[32m+[m[32m  if (!scene.image_prompt) {[m
[32m+[m[32m    throw new Error(`Scene ${scene.scene_order} has no image_prompt`);[m
[32m+[m[32m  }[m
[32m+[m
   const prompt = `${STYLE_LOCK} The main character is ${CHARACTER_DESCRIPTION}. Scene: ${scene.image_prompt}`;[m
[32m+[m[32m  const finalPath = path.join(outputDir, `scene-${scene.scene_order}.png`);[m
 [m
   for (let attempt = 1; attempt <= 3; attempt++) {[m
     try {[m
[31m-      await generateFluxImage(prompt, destPath);[m
[31m-      return destPath;[m
[32m+[m[32m      await generateFluxImage(prompt, finalPath);[m
[32m+[m[32m      return { filePath: finalPath, source: `pollinations-${IMAGE_MODEL}` };[m
     } catch (err) {[m
       console.warn(`Image attempt ${attempt}/3 failed for scene ${scene.scene_order}: ${err.message}`);[m
       if (attempt < 3) {[m
         const backoff = Math.min(8000, 1500 * Math.pow(2, attempt - 1));[m
         await new Promise((r) => setTimeout(r, backoff));[m
[31m-      } else {[m
[31m-        return null;[m
       }[m
     }[m
   }[m
[31m-  return null;[m
[31m-}[m
[31m-[m
[31m-async function pickBestCandidate(scene, candidatePaths) {[m
[31m-  if (!GEMINI_API_KEY) return 0;[m
[31m-[m
[31m-  const imageParts = candidatePaths.map(fileToInlinePart);[m
[31m-  const prompt =[m
[31m-    `You are judging ${candidatePaths.length} candidate illustrations for the same video scene, ` +[m
[31m-    `shown in order (Image 1, Image 2, ...). Scene description: "${scene.image_prompt}". ` +[m
[31m-    `Pick the image that best matches the scene description and has the clearest composition. ` +[m
[31m-    `Respond with ONLY a JSON object like {"best": 1} - no explanation, no markdown.`;[m
[31m-[m
[31m-  const res = await fetch(`${GEMINI_EVAL_URL}?key=${GEMINI_API_KEY}`, {[m
[31m-    method: 'POST',[m
[31m-    headers: { 'Content-Type': 'application/json' },[m
[31m-    body: JSON.stringify({ contents: [{ parts: [...imageParts, { text: prompt }] }] }),[m
[31m-    signal: AbortSignal.timeout(60000),[m
[31m-  });[m
[31m-[m
[31m-  if (!res.ok) {[m
[31m-    const body = await res.text().catch(() => '');[m
[31m-    throw new Error(`Evaluation request failed: ${res.status} ${body.slice(0, 200)}`);[m
[31m-  }[m
[31m-[m
[31m-  const data = await res.json();[m
[31m-  const text = data?.candidates?.[0]?.content?.parts?.find((p) => p.text)?.text || '';[m
[31m-  const match = text.match(/"best"\s*:\s*(\d+)/) || text.match(/\d+/);[m
[31m-  if (!match) throw new Error(`Could not parse evaluation response: ${text.slice(0, 150)}`);[m
[31m-[m
[31m-  const chosen = parseInt(match[1] || match[0], 10);[m
[31m-  if (!chosen || chosen < 1 || chosen > candidatePaths.length) {[m
[31m-    throw new Error(`Evaluation returned out-of-range choice: ${chosen}`);[m
[31m-  }[m
[31m-  console.log(`  -> Scene ${scene.scene_order}: picked candidate ${chosen}/${candidatePaths.length}`);[m
[31m-  return chosen - 1;[m
[31m-}[m
[31m-[m
[31m-async function generateSceneImageBestOf(scene, outputDir) {[m
[31m-  if (!scene.image_prompt) {[m
[31m-    throw new Error(`Scene ${scene.scene_order} has no image_prompt`);[m
[31m-  }[m
[31m-[m
[31m-  const candidatePaths = [];[m
[31m-  for (let i = 0; i < CANDIDATE_COUNT; i++) {[m
[31m-    const candDest = path.join(outputDir, `scene-${scene.scene_order}-cand${i + 1}.png`);[m
[31m-    const result = await generateOneCandidate(scene, candDest);[m
[31m-    if (result) candidatePaths.push(result);[m
[31m-  }[m
[31m-[m
[31m-  if (candidatePaths.length === 0) {[m
[31m-    return { filePath: null, source: null };[m
[31m-  }[m
[31m-[m
[31m-  const finalPath = path.join(outputDir, `scene-${scene.scene_order}.png`);[m
[31m-[m
[31m-  if (candidatePaths.length === 1) {[m
[31m-    fs.renameSync(candidatePaths[0], finalPath);[m
[31m-    return { filePath: finalPath, source: `pollinations-${IMAGE_MODEL}` };[m
[31m-  }[m
[31m-[m
[31m-  let bestIndex = 0;[m
[31m-  try {[m
[31m-    bestIndex = await pickBestCandidate(scene, candidatePaths);[m
[31m-  } catch (err) {[m
[31m-    console.warn(`  -> Evaluation failed for scene ${scene.scene_order}, keeping first candidate: ${err.message}`);[m
[31m-  }[m
[31m-[m
[31m-  candidatePaths.forEach((p, i) => {[m
[31m-    if (i === bestIndex) {[m
[31m-      fs.renameSync(p, finalPath);[m
[31m-    } else {[m
[31m-      try { fs.unlinkSync(p); } catch {}[m
[31m-    }[m
[31m-  });[m
[31m-[m
[31m-  return { filePath: finalPath, source: `pollinations-${IMAGE_MODEL}` };[m
[32m+[m[32m  return { filePath: null, source: null };[m
 }[m
 [m
 async function getAllSceneImages(scenes, outputDir) {[m
[36m@@ -199,13 +143,13 @@[m [masync function getAllSceneImages(scenes, outputDir) {[m
 [m
   for (let i = 0; i < scenes.length; i += BATCH_SIZE) {[m
     const batch = scenes.slice(i, i + BATCH_SIZE);[m
[31m-    console.log(`  -> Generating images ${i + 1}-${Math.min(i + BATCH_SIZE, scenes.length)}/${scenes.length} (${CANDIDATE_COUNT} candidate(s) each)...`);[m
[32m+[m[32m    console.log(`  -> Generating images ${i + 1}-${Math.min(i + BATCH_SIZE, scenes.length)}/${scenes.length}...`);[m
     const batchResults = await Promise.all([m
       batch.map(async (scene, idx) => {[m
         await new Promise((r) => setTimeout(r, idx * 500));[m
         try {[m
           const { filePath, source } = await generateSceneImageBestOf(scene, outputDir);[m
[31m-          return { filePath, source, error: filePath ? null : 'All candidates failed' };[m
[32m+[m[32m          return { filePath, source, error: filePath ? null : 'Image generation failed' };[m
         } catch (err) {[m
           return { filePath: null, source: null, error: err.message };[m
         }[m
[1mdiff --git a/services/tts.service.js b/services/tts.service.js[m
[1mindex a0361c4..2b537d0 100644[m
[1m--- a/services/tts.service.js[m
[1m+++ b/services/tts.service.js[m
[36m@@ -7,6 +7,25 @@[m [mconst path = require('path');[m
 const DEFAULT_VOICE = process.env.TTS_VOICE || 'en-US-EricNeural';[m
 const SCENES_PER_BATCH = 8;[m
 [m
[32m+[m[32m// Rotates narrator voice based on the chosen narrative style, for variety[m
[32m+[m[32m// across videos instead of one fixed voice every time.[m
[32m+[m[32mconst VOICE_POOL = [[m
[32m+[m[32m  'en-US-EricNeural',[m
[32m+[m[32m  'en-US-GuyNeural',[m
[32m+[m[32m  'en-US-DavisNeural',[m
[32m+[m[32m  'en-US-TonyNeural',[m
[32m+[m[32m  'en-US-ChristopherNeural',[m
[32m+[m[32m];[m
[32m+[m
[32m+[m[32mfunction pickVoiceForStyle(narrativeStyle) {[m
[32m+[m[32m  if (!narrativeStyle) return DEFAULT_VOICE;[m
[32m+[m[32m  let hash = 0;[m
[32m+[m[32m  for (let i = 0; i < narrativeStyle.length; i++) {[m
[32m+[m[32m    hash = (hash * 31 + narrativeStyle.charCodeAt(i)) >>> 0;[m
[32m+[m[32m  }[m
[32m+[m[32m  return VOICE_POOL[hash % VOICE_POOL.length];[m
[32m+[m[32m}[m
[32m+[m
 function buildBatchText(scenes) {[m
   return scenes.map((scene) => scene.text).join(' ... ');[m
 }[m
[36m@@ -30,13 +49,16 @@[m [mfunction runFfmpeg(args) {[m
 async function generateNarrationAudio(scenes, options = {}) {[m
   const {[m
     outputPath = path.join('/tmp', `narration-${Date.now()}.mp3`),[m
[31m-    voice = DEFAULT_VOICE,[m
[32m+[m[32m    narrativeStyle = null,[m
[32m+[m[32m    voice = options.voice || pickVoiceForStyle(narrativeStyle),[m
   } = options;[m
 [m
   if (!Array.isArray(scenes) || scenes.length === 0) {[m
     throw new Error('No scenes provided for narration');[m
   }[m
 [m
[32m+[m[32m  console.log(`  -> Using narrator voice: ${voice}`);[m
[32m+[m
   const batchDir = path.join('/tmp', `tts-batches-${Date.now()}`);[m
   fs.mkdirSync(batchDir, { recursive: true });[m
 [m
